/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
}

module.exports = {
  // other exports
  // nextConfig,
  reactStrictMode: true,
  target: 'serverless', 
};

module.exports = nextConfig
